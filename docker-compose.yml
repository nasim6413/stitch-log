services:
  stitch-log:
    container_name: stitch-log
    build:
      context: .
      dockerfile: Dockerfile
      no_cache: true
    image: stitch-log
    restart: unless-stopped    
    ports:
      - "80:80"
    volumes:
    - ./instance:/stitch-log/instance